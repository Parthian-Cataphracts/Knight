# Reverse Proxy

Reserved for the future reverse-proxy configuration responsible for routing
custom tenant domains to the central API and, later, to independently deployed
tenant storefronts. No routing rules exist yet — see
`docs/architecture/multi-tenancy.md` for the intended custom-domain resolution
model this will implement.
