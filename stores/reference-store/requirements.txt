# The reference store deliberately depends on very little. Everything here is
# needed by the store itself; the KNIGHT integration layer adds `requests` and,
# for verifying the signature on a delivered feature artifact, `cryptography`.
# The second is not optional: an installer that cannot verify a signature must
# refuse to install, so a store without it can receive no features at all.
Django==5.1.15
djangorestframework==3.15.2
psycopg[binary]==3.2.3
requests==2.32.5
cryptography==43.0.1
