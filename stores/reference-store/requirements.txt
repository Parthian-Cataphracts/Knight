# The reference store deliberately depends on very little. Everything here is
# needed by the store itself; the KNIGHT integration layer adds only `requests`.
Django==5.1.15
djangorestframework==3.15.2
psycopg[binary]==3.2.3
requests==2.32.5
