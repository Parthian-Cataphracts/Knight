# Test-only dependencies for the reference store.
#
# Kept apart from requirements.txt so that what a store actually runs in
# production stays visible and minimal. The contract tests validate payloads
# against docs/contracts/store-integration.schema.json and need a JSON Schema
# implementation to do it; nothing in the store itself does.
-r requirements.txt

jsonschema==4.23.0

# Parses a Feature's knight_manifest.yaml for `manage.py knight_install_local`.
# The store depends on it nowhere in production - it reads job payloads, which
# are JSON - so it is a development dependency. Without it that command falls
# back to a partial parser, and a partial parser reading a contract is how a
# Feature's routes went unregistered for two phases.
PyYAML>=6.0,<7.0
