"""
The KNIGHT agent: telemetry and typed job execution on a managed server.

Deliberately tiny and dependency-free. It runs on infrastructure KNIGHT does not
own, so every package it needs is a package somebody else has to install, keep
current and trust.
"""

__version__ = "1.0.0"
